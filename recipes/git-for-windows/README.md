# Install Git for Windows within an existing MSYS2 environment. 
The code is taken from https://gitforwindows.org/install-inside-msys2-proper.html
In addition, an application user is established to manage SSL keys and environment settings.