---
  version: 1.0

  platform: mingw64

  target: ucrt64

  msys2-version: ">3.6.5"

  steps:
    - name: "#1"
      description: "Add UCRT (Microsoft's 'universal common runtime') 64-bit toolchain, including GDB (GNU C(++) debugger."
      packages:
        - "base-devel"
        - "mingw-w64-ucrt-x86_64-binutils"
        - "mingw-w64-ucrt-x86_64-crt-git"
        - "mingw-w64-ucrt-x86_64-gcc"
        - "mingw-w64-ucrt-x86_64-gdb"
        - "mingw-w64-ucrt-x86_64-gdb-multiarch"
        - "mingw-w64-ucrt-x86_64-headers-git"
        - "mingw-w64-ucrt-x86_64-libmangle-git"
        - "mingw-w64-ucrt-x86_64-libwinpthread"
        - "mingw-w64-ucrt-x86_64-winpthreads"
        - "mingw-w64-ucrt-x86_64-make"
        - "mingw-w64-ucrt-x86_64-pkgconf"
        - "mingw-w64-ucrt-x86_64-tools-git"
        - "mingw-w64-ucrt-x86_64-winstorecompat-git"

    - name: '#2'
      description: "Update MSYS2 packages database (sync)"
      commands:
        - "sh -c 'pacman -Suu --noconfirm'"

